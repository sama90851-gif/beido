name: beido
description: تطبيق بيدو - رفيقتكِ الذكية والمريحة.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.1   # ✅ أضفناها هنا

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/beido_logo.png"
  adaptive_icon_background: "#FFF5F7"

flutter:
  uses-material-design: true
